% Develop Matlab scripts to generate the discrete-time sequences 
% (i) Unit impulse x[n] = ?(n) 
% (ii) Unit step x[n] = µ(n) 
% (iii) Real exponential x[n] = exp[n] 
% (iv) Complex exponential x[n] = exp[(5j)n]. 
% Plot all these sequences in the same figure.

% Develop a Matlab function for complex exponential of fundamental
% period T0. Use this function to generate sinusoidal signals. Plot the
% sinusoidal.

% Generate a normal random sequence of size N = 106 and save the
% data in a .mat file. Extract the data from.mat file and plot for comparison.

% Create an excel sheet and write 10 random numbers in two columns.
% Import the excel sheet data in Matlab.

% Create comma-separated value (CSV) file and write 10 random
% numbers. Import the data in Matlab.